-- zenline.nvim - Default Configuration Loader
-- This file maintains backwards compatibility by loading the normal default
-- Users can override by passing custom config to setup()

return require("zenline.defaults.normal")
